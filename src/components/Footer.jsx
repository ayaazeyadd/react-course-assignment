import React from "react";

function Footer() {
  return (
    <div className="app-footer">
      Built with React &amp; Bootstrap &middot; {new Date().getFullYear()} Student Dashboard
    </div>
  );
}

export default Footer;
